import { StyleSheet } from 'react-native';
import { WIDTH } from '../../core/constants';

export default StyleSheet.create( {
  container: {
    overflow: 'hidden',
    width: WIDTH,
    flex: 1
  },
} );
