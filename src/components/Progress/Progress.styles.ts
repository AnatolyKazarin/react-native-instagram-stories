import { StyleSheet } from 'react-native';

export default StyleSheet.create( {
  container: {
    height: 2,
    flexDirection: 'row',
    gap: 4,
    marginLeft: 16,
    marginTop: 10
  },
  item: {
    height: 3,
    borderRadius: 8,
    overflow: 'hidden',
  },
} );
